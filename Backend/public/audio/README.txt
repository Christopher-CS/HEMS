Place audio files here to serve them at GET /audio/<filename>.

Example used by the catalog (track-001):
  sample.mp3  →  http://<host>:4000/audio/sample.mp3

Supported formats depend on the mobile player (expo-av); MP3 is a safe default.

Do not commit licensed music you are not allowed to redistribute.
